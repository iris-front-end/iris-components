import styled from 'styled-components';

export const HeaderWrapper = styled.div`
  display: flex;
  flex-wrap: wrap;
  justify-content: center;

  && > * {
    padding-left: 10px;
    padding-right: 10px;
  }
`;
