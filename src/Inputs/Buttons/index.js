import ButtonComponent from './Button';
import CancelAndConfirm from './CancelAndConfirm';
import IconButton from './IconButton';

const Buttons = {
  ButtonComponent,
  CancelAndConfirm,
  IconButton,
};

export default Buttons;
