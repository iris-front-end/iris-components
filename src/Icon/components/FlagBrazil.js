import styled from 'styled-components';

import flagBrazil from 'assets/img/br.png';

export const FlagBrazil = styled.i`
  background-image: url(${flagBrazil});
  background-size: cover;
  border-radius: 4px;
  height: 25px;
  width: 36px;
`;
